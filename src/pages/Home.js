import MainLayout from '../components/Layout/MainLayout';
import Promotion from '../components/UI/Promotion';

const Home = () => {
  return (
    <MainLayout>
      <Promotion/>
    </MainLayout>
  );
};

export default Home;
