import Container from '@components/UI/Container';
import CartScreen from '@features/CartScreen';

function Cart() {
	return (
		<Container>
			<CartScreen />
		</Container>
	);
}

export default Cart;
